<%= form_for(current_user.active_relationships.build, remote: true, class: "form-inline") do |f| %>
	<%= hidden_field_tag :followed_id, @user.id %>

	<%= button_tag  type: "submit", class: "btn btn-primary btn-block" do %>
		<span class='glyphicon glyphicon-tags'> Follow</span>
	<% end %>
<% end %>

