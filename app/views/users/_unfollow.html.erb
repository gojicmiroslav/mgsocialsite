<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
              html: { method: :delete}, remote: true, class: "form-inline") do |f| %>

	<%= button_tag  type: "submit", class: "btn btn-danger btn-block" do %>
		<span>Unfollow</span>
	<% end %>

<% end %>



